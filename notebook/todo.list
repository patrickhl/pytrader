# This is the to-do list for developing the pytrader
# mark can be: 
# P*: tasks' priority
# DONE: finished
# PEND: started but not finished
# TODO: new task
# Date: updated date

# Task list:

1, [P0,DONE,SEP23] create functions to fetch data in a trading day
    a, functions are finished, copy and cleanup code
       [comment]: finished two funcs for getting inday data and money flow
2, [P0,TODO,SEP20] create functions to fetch history k-day data, save in files, not database
    a, most are finished, need to collect and cleanup
3, [P0,TODO,SEP20] create functions to call wechat
    a, give a simple test first
    b, send summary everyday  
4, [p1,TODO,SEP20] eval model to select candidates
5, [p1,TODO,SEP20] eval model to decide sell price
6, [p2,TODO,SEP20] create routine arch
    a, give a simple test first
    b, add feature
